function dateFormat(unformattedDate) {

}

expect(dateFormat('January 2nd 2011')).toBe('01/02/2011')

// that test will never run it is not suppose to

//the idea is to start write test before testing